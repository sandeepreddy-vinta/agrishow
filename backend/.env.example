# Environment Configuration for FranchiseOS Backend
# Copy this file to .env and fill in your values
# For Cloud Run deployment, set these as Environment Variables in Cloud Run Dashboard

# ============ REQUIRED ============
API_KEY=your-secure-api-key-here
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=YourSecurePassword123!

# ============ GOOGLE CLOUD STORAGE (Required for file uploads) ============
GCS_BUCKET_NAME=your-gcs-bucket-name

# ============ GOOGLE CLOUD AUTH (Required for local development) ============
# Path to your service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# ============ OPTIONAL (with defaults) ============
PORT=3000
JWT_EXPIRES_IN=24h
MAX_FILE_SIZE=524288000
ALLOWED_VIDEO_TYPES=video/mp4,video/quicktime,video/webm
ALLOWED_IMAGE_TYPES=image/jpeg,image/png

# Add your admin dashboard URL
ALLOWED_ORIGINS=https://your-admin-dashboard.netlify.app

# ============ PRODUCTION ============
NODE_ENV=production
